---
apiVersion: v1
kind: Service
metadata:
  name: keep-dapp-token-dashboard
  labels:
    app: keep-dapp
    type: keep-token-dashboard
spec:
  ports:
  - port: 80
    targetPort: 80
    name: tcp-80
  selector:
    app: keep-dapp
    type: keep-token-dashboard