---
apiVersion: v1
kind: Service
metadata:
  name: keep-token-dashboard-dapp
  labels:
    app: keep-token-dashboard
    type: dapp
spec:
  ports:
  - port: 80
    targetPort: 80
    name: tcp-80
  selector:
    app: keep-token-dashboard
    type: dapp