# File generated with gen.sh - DO NOT EDIT
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: keep-client-0
  namespace: default
  labels:
    app: keep
    type: client
    id: "0"
    network: goerli
spec:
  replicas: 1
  selector:
    matchLabels:
      app: keep
      type: client
      id: "0"
      network: goerli
  serviceName: keep-client-0
  volumeClaimTemplates:
  - metadata:
      name: keep-client-data
    spec:
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: 512Mi
  template:
    metadata:
      labels:
        app: keep
        type: client
        id: "0"
        network: goerli
    spec:
      volumes:
      - name: keep-client-data
        persistentVolumeClaim:
          claimName: keep-client-data
      - name: eth-account-keyfile
        configMap:
          name: eth-account-info
          items:
          - key: account-0-keyfile
            path: account-0-keyfile
      containers:
      - name: keep-client
        image: gcr.io/keep-test-f3e0/keep-client:latest
        imagePullPolicy: Always
        ports:
        - name: network
          containerPort: 3919
        - name: metrics
          containerPort: 9601
        - name: diagnostics
          containerPort: 9701
        env:
        - name: KEEP_ETHEREUM_PASSWORD
          valueFrom:
            secretKeyRef:
              name: eth-account-passphrases
              key: account-0
        - name: ETH_WS_URL
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: ws-url
        envFrom:
        - configMapRef:
            name: keep-client-config
        volumeMounts:
        - name: keep-client-data
          mountPath: /mnt/keep-client/data
        - name: eth-account-keyfile
          mountPath: /mnt/keep-client/keyfile
        command:
        - keep-client
        - start
        args:
        - --goerli
        - --ethereum.url
        - $(ETH_WS_URL)
        - --ethereum.keyFile
        - /mnt/keep-client/keyfile/account-0-keyfile
        - --storage.dir
        - /mnt/keep-client/data
        - --network.port
        - "3919"
        - --network.announcedAddresses
        - /dns4/keep-client-0.default.svc.cluster.local/tcp/3919,/dns4/bootstrap-0.test.keep.network/tcp/3919
        - --metrics.port
        - "9601"
        - --diagnostics.port
        - "9701"
      initContainers:
      - name: initcontainer-beacon
        image: gcr.io/keep-test-f3e0/keep-random-beacon-hardhat:latest
        imagePullPolicy: Always
        env:
        - name: CHAIN_API_URL
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: http-url
        - name: CONTRACT_OWNER_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: contract-owner-eth-account-private-key
        - name: KEEP_CLIENT_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-account-privatekeys
              key: account-0
        - name: ACCOUNTS_PRIVATE_KEYS
          value: $(CONTRACT_OWNER_ETH_PRIVATE_KEY),$(KEEP_CLIENT_ETH_PRIVATE_KEY)
        - name: KEEP_CLIENT_ETH_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: eth-account-info
              key: account-0-address
        args:
        - initialize
        - --network
        - goerli
        - --owner
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --provider
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --operator
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --amount
        - "800000"
        - --authorization
        - "800000"
      - name: initcontainer-ecdsa
        image: gcr.io/keep-test-f3e0/keep-ecdsa-hardhat:latest
        imagePullPolicy: Always
        env:
        - name: CHAIN_API_URL
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: http-url
        - name: CONTRACT_OWNER_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: contract-owner-eth-account-private-key
        - name: KEEP_CLIENT_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-account-privatekeys
              key: account-0
        - name: ACCOUNTS_PRIVATE_KEYS
          value: $(CONTRACT_OWNER_ETH_PRIVATE_KEY),$(KEEP_CLIENT_ETH_PRIVATE_KEY)
        - name: KEEP_CLIENT_ETH_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: eth-account-info
              key: account-0-address
        args:
        - initialize
        - --network
        - goerli
        - --owner
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --provider
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --operator
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --amount
        - "800000"
        - --authorization
        - "800000"
---
apiVersion: v1
kind: Service
metadata:
  name: keep-client-0
  namespace: default
  labels:
    app: keep
    type: client
    id: "0"
    network: goerli
spec:
  type: LoadBalancer
  ports:
  - name: network
    port: 3919
    targetPort: network
  - name: metrics
    port: 9601
    targetPort: metrics
  - name: diagnostics
    port: 9701
    targetPort: diagnostics
  selector:
    app: keep
    type: client
    id: "0"
    network: goerli
  loadBalancerIP: 104.154.61.116
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: keep-client-1
  namespace: default
  labels:
    app: keep
    type: client
    id: "1"
    network: goerli
spec:
  replicas: 1
  selector:
    matchLabels:
      app: keep
      type: client
      id: "1"
      network: goerli
  serviceName: keep-client-1
  volumeClaimTemplates:
  - metadata:
      name: keep-client-data
    spec:
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: 512Mi
  template:
    metadata:
      labels:
        app: keep
        type: client
        id: "1"
        network: goerli
    spec:
      volumes:
      - name: keep-client-data
        persistentVolumeClaim:
          claimName: keep-client-data
      - name: eth-account-keyfile
        configMap:
          name: eth-account-info
          items:
          - key: account-1-keyfile
            path: account-1-keyfile
      containers:
      - name: keep-client
        image: gcr.io/keep-test-f3e0/keep-client:latest
        imagePullPolicy: Always
        ports:
        - name: network
          containerPort: 3919
        - name: metrics
          containerPort: 9601
        - name: diagnostics
          containerPort: 9701
        env:
        - name: KEEP_ETHEREUM_PASSWORD
          valueFrom:
            secretKeyRef:
              name: eth-account-passphrases
              key: account-1
        - name: ETH_WS_URL
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: ws-url
        envFrom:
        - configMapRef:
            name: keep-client-config
        volumeMounts:
        - name: keep-client-data
          mountPath: /mnt/keep-client/data
        - name: eth-account-keyfile
          mountPath: /mnt/keep-client/keyfile
        command:
        - keep-client
        - start
        args:
        - --goerli
        - --ethereum.url
        - $(ETH_WS_URL)
        - --ethereum.keyFile
        - /mnt/keep-client/keyfile/account-1-keyfile
        - --storage.dir
        - /mnt/keep-client/data
        - --network.port
        - "3919"
        - --network.announcedAddresses
        - /dns4/keep-client-1.default.svc.cluster.local/tcp/3919,/dns4/bootstrap-1.test.keep.network/tcp/3919
        - --network.peers
        - /dns4/keep-client-0.default.svc.cluster.local/tcp/3919/ipfs/16Uiu2HAmCcfVpHwfBKNFbQuhvGuFXHVLQ65gB4sJm7HyrcZuLttH
        - --metrics.port
        - "9601"
        - --diagnostics.port
        - "9701"
      initContainers:
      - name: initcontainer-beacon
        image: gcr.io/keep-test-f3e0/keep-random-beacon-hardhat:latest
        imagePullPolicy: Always
        env:
        - name: CHAIN_API_URL
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: http-url
        - name: CONTRACT_OWNER_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: contract-owner-eth-account-private-key
        - name: KEEP_CLIENT_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-account-privatekeys
              key: account-1
        - name: ACCOUNTS_PRIVATE_KEYS
          value: $(CONTRACT_OWNER_ETH_PRIVATE_KEY),$(KEEP_CLIENT_ETH_PRIVATE_KEY)
        - name: KEEP_CLIENT_ETH_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: eth-account-info
              key: account-1-address
        args:
        - initialize
        - --network
        - goerli
        - --owner
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --provider
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --operator
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --amount
        - "800000"
        - --authorization
        - "800000"
      - name: initcontainer-ecdsa
        image: gcr.io/keep-test-f3e0/keep-ecdsa-hardhat:latest
        imagePullPolicy: Always
        env:
        - name: CHAIN_API_URL
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: http-url
        - name: CONTRACT_OWNER_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: contract-owner-eth-account-private-key
        - name: KEEP_CLIENT_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-account-privatekeys
              key: account-1
        - name: ACCOUNTS_PRIVATE_KEYS
          value: $(CONTRACT_OWNER_ETH_PRIVATE_KEY),$(KEEP_CLIENT_ETH_PRIVATE_KEY)
        - name: KEEP_CLIENT_ETH_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: eth-account-info
              key: account-1-address
        args:
        - initialize
        - --network
        - goerli
        - --owner
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --provider
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --operator
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --amount
        - "800000"
        - --authorization
        - "800000"
---
apiVersion: v1
kind: Service
metadata:
  name: keep-client-1
  namespace: default
  labels:
    app: keep
    type: client
    id: "1"
    network: goerli
spec:
  type: LoadBalancer
  ports:
  - name: network
    port: 3919
    targetPort: network
  - name: metrics
    port: 9601
    targetPort: metrics
  - name: diagnostics
    port: 9701
    targetPort: diagnostics
  selector:
    app: keep
    type: client
    id: "1"
    network: goerli
  loadBalancerIP: 35.223.100.87
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: keep-client-2
  namespace: default
  labels:
    app: keep
    type: client
    id: "2"
    network: goerli
spec:
  replicas: 1
  selector:
    matchLabels:
      app: keep
      type: client
      id: "2"
      network: goerli
  serviceName: keep-client-2
  volumeClaimTemplates:
  - metadata:
      name: keep-client-data
    spec:
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: 512Mi
  template:
    metadata:
      labels:
        app: keep
        type: client
        id: "2"
        network: goerli
    spec:
      volumes:
      - name: keep-client-data
        persistentVolumeClaim:
          claimName: keep-client-data
      - name: eth-account-keyfile
        configMap:
          name: eth-account-info
          items:
          - key: account-2-keyfile
            path: account-2-keyfile
      containers:
      - name: keep-client
        image: gcr.io/keep-test-f3e0/keep-client:latest
        imagePullPolicy: Always
        ports:
        - name: network
          containerPort: 3919
        - name: metrics
          containerPort: 9601
        - name: diagnostics
          containerPort: 9701
        env:
        - name: KEEP_ETHEREUM_PASSWORD
          valueFrom:
            secretKeyRef:
              name: eth-account-passphrases
              key: account-2
        - name: ETH_WS_URL
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: ws-url
        envFrom:
        - configMapRef:
            name: keep-client-config
        volumeMounts:
        - name: keep-client-data
          mountPath: /mnt/keep-client/data
        - name: eth-account-keyfile
          mountPath: /mnt/keep-client/keyfile
        command:
        - keep-client
        - start
        args:
        - --goerli
        - --ethereum.url
        - $(ETH_WS_URL)
        - --ethereum.keyFile
        - /mnt/keep-client/keyfile/account-2-keyfile
        - --storage.dir
        - /mnt/keep-client/data
        - --network.port
        - "3919"
        - --network.announcedAddresses
        - /dns4/keep-client-2.default.svc.cluster.local/tcp/3919
        - --network.peers
        - /dns4/keep-client-1.default.svc.cluster.local/tcp/3919/ipfs/16Uiu2HAm3eJtyFKAttzJ85NLMromHuRg4yyum3CREMf6CHBBV6KY
        - --metrics.port
        - "9601"
        - --diagnostics.port
        - "9701"
      initContainers:
      - name: initcontainer-beacon
        image: gcr.io/keep-test-f3e0/keep-random-beacon-hardhat:latest
        imagePullPolicy: Always
        env:
        - name: CHAIN_API_URL
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: http-url
        - name: CONTRACT_OWNER_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: contract-owner-eth-account-private-key
        - name: KEEP_CLIENT_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-account-privatekeys
              key: account-2
        - name: ACCOUNTS_PRIVATE_KEYS
          value: $(CONTRACT_OWNER_ETH_PRIVATE_KEY),$(KEEP_CLIENT_ETH_PRIVATE_KEY)
        - name: KEEP_CLIENT_ETH_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: eth-account-info
              key: account-2-address
        args:
        - initialize
        - --network
        - goerli
        - --owner
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --provider
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --operator
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --amount
        - "800000"
        - --authorization
        - "800000"
      - name: initcontainer-ecdsa
        image: gcr.io/keep-test-f3e0/keep-ecdsa-hardhat:latest
        imagePullPolicy: Always
        env:
        - name: CHAIN_API_URL
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: http-url
        - name: CONTRACT_OWNER_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: contract-owner-eth-account-private-key
        - name: KEEP_CLIENT_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-account-privatekeys
              key: account-2
        - name: ACCOUNTS_PRIVATE_KEYS
          value: $(CONTRACT_OWNER_ETH_PRIVATE_KEY),$(KEEP_CLIENT_ETH_PRIVATE_KEY)
        - name: KEEP_CLIENT_ETH_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: eth-account-info
              key: account-2-address
        args:
        - initialize
        - --network
        - goerli
        - --owner
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --provider
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --operator
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --amount
        - "800000"
        - --authorization
        - "800000"
---
apiVersion: v1
kind: Service
metadata:
  name: keep-client-2
  namespace: default
  labels:
    app: keep
    type: client
    id: "2"
    network: goerli
spec:
  type: LoadBalancer
  ports:
  - name: network
    port: 3919
    targetPort: network
  - name: metrics
    port: 9601
    targetPort: metrics
  - name: diagnostics
    port: 9701
    targetPort: diagnostics
  selector:
    app: keep
    type: client
    id: "2"
    network: goerli
  loadBalancerIP: null
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: keep-client-3
  namespace: default
  labels:
    app: keep
    type: client
    id: "3"
    network: goerli
spec:
  replicas: 1
  selector:
    matchLabels:
      app: keep
      type: client
      id: "3"
      network: goerli
  serviceName: keep-client-3
  volumeClaimTemplates:
  - metadata:
      name: keep-client-data
    spec:
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: 512Mi
  template:
    metadata:
      labels:
        app: keep
        type: client
        id: "3"
        network: goerli
    spec:
      volumes:
      - name: keep-client-data
        persistentVolumeClaim:
          claimName: keep-client-data
      - name: eth-account-keyfile
        configMap:
          name: eth-account-info
          items:
          - key: account-3-keyfile
            path: account-3-keyfile
      containers:
      - name: keep-client
        image: gcr.io/keep-test-f3e0/keep-client:latest
        imagePullPolicy: Always
        ports:
        - name: network
          containerPort: 3919
        - name: metrics
          containerPort: 9601
        - name: diagnostics
          containerPort: 9701
        env:
        - name: KEEP_ETHEREUM_PASSWORD
          valueFrom:
            secretKeyRef:
              name: eth-account-passphrases
              key: account-3
        - name: ETH_WS_URL
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: ws-url
        envFrom:
        - configMapRef:
            name: keep-client-config
        volumeMounts:
        - name: keep-client-data
          mountPath: /mnt/keep-client/data
        - name: eth-account-keyfile
          mountPath: /mnt/keep-client/keyfile
        command:
        - keep-client
        - start
        args:
        - --goerli
        - --ethereum.url
        - $(ETH_WS_URL)
        - --ethereum.keyFile
        - /mnt/keep-client/keyfile/account-3-keyfile
        - --storage.dir
        - /mnt/keep-client/data
        - --network.port
        - "3919"
        - --network.announcedAddresses
        - /dns4/keep-client-3.default.svc.cluster.local/tcp/3919
        - --network.peers
        - /dns4/keep-client-2.default.svc.cluster.local/tcp/3919/ipfs/16Uiu2HAmNNuCp45z5bgB8KiTHv1vHTNAVbBgxxtTFGAndageo9Dp
        - --metrics.port
        - "9601"
        - --diagnostics.port
        - "9701"
      initContainers:
      - name: initcontainer-beacon
        image: gcr.io/keep-test-f3e0/keep-random-beacon-hardhat:latest
        imagePullPolicy: Always
        env:
        - name: CHAIN_API_URL
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: http-url
        - name: CONTRACT_OWNER_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: contract-owner-eth-account-private-key
        - name: KEEP_CLIENT_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-account-privatekeys
              key: account-3
        - name: ACCOUNTS_PRIVATE_KEYS
          value: $(CONTRACT_OWNER_ETH_PRIVATE_KEY),$(KEEP_CLIENT_ETH_PRIVATE_KEY)
        - name: KEEP_CLIENT_ETH_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: eth-account-info
              key: account-3-address
        args:
        - initialize
        - --network
        - goerli
        - --owner
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --provider
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --operator
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --amount
        - "800000"
        - --authorization
        - "800000"
      - name: initcontainer-ecdsa
        image: gcr.io/keep-test-f3e0/keep-ecdsa-hardhat:latest
        imagePullPolicy: Always
        env:
        - name: CHAIN_API_URL
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: http-url
        - name: CONTRACT_OWNER_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: contract-owner-eth-account-private-key
        - name: KEEP_CLIENT_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-account-privatekeys
              key: account-3
        - name: ACCOUNTS_PRIVATE_KEYS
          value: $(CONTRACT_OWNER_ETH_PRIVATE_KEY),$(KEEP_CLIENT_ETH_PRIVATE_KEY)
        - name: KEEP_CLIENT_ETH_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: eth-account-info
              key: account-3-address
        args:
        - initialize
        - --network
        - goerli
        - --owner
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --provider
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --operator
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --amount
        - "800000"
        - --authorization
        - "800000"
---
apiVersion: v1
kind: Service
metadata:
  name: keep-client-3
  namespace: default
  labels:
    app: keep
    type: client
    id: "3"
    network: goerli
spec:
  type: LoadBalancer
  ports:
  - name: network
    port: 3919
    targetPort: network
  - name: metrics
    port: 9601
    targetPort: metrics
  - name: diagnostics
    port: 9701
    targetPort: diagnostics
  selector:
    app: keep
    type: client
    id: "3"
    network: goerli
  loadBalancerIP: null
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: keep-client-4
  namespace: default
  labels:
    app: keep
    type: client
    id: "4"
    network: goerli
spec:
  replicas: 1
  selector:
    matchLabels:
      app: keep
      type: client
      id: "4"
      network: goerli
  serviceName: keep-client-4
  volumeClaimTemplates:
  - metadata:
      name: keep-client-data
    spec:
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: 512Mi
  template:
    metadata:
      labels:
        app: keep
        type: client
        id: "4"
        network: goerli
    spec:
      volumes:
      - name: keep-client-data
        persistentVolumeClaim:
          claimName: keep-client-data
      - name: eth-account-keyfile
        configMap:
          name: eth-account-info
          items:
          - key: account-4-keyfile
            path: account-4-keyfile
      containers:
      - name: keep-client
        image: gcr.io/keep-test-f3e0/keep-client:latest
        imagePullPolicy: Always
        ports:
        - name: network
          containerPort: 3919
        - name: metrics
          containerPort: 9601
        - name: diagnostics
          containerPort: 9701
        env:
        - name: KEEP_ETHEREUM_PASSWORD
          valueFrom:
            secretKeyRef:
              name: eth-account-passphrases
              key: account-4
        - name: ETH_WS_URL
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: ws-url
        envFrom:
        - configMapRef:
            name: keep-client-config
        volumeMounts:
        - name: keep-client-data
          mountPath: /mnt/keep-client/data
        - name: eth-account-keyfile
          mountPath: /mnt/keep-client/keyfile
        command:
        - keep-client
        - start
        args:
        - --goerli
        - --ethereum.url
        - $(ETH_WS_URL)
        - --ethereum.keyFile
        - /mnt/keep-client/keyfile/account-4-keyfile
        - --storage.dir
        - /mnt/keep-client/data
        - --network.port
        - "3919"
        - --network.announcedAddresses
        - /dns4/keep-client-4.default.svc.cluster.local/tcp/3919
        - --network.peers
        - /dns4/keep-client-3.default.svc.cluster.local/tcp/3919/ipfs/16Uiu2HAm8KJX32kr3eYUhDuzwTucSfAfspnjnXNf9veVhB12t6Vf
        - --metrics.port
        - "9601"
        - --diagnostics.port
        - "9701"
      initContainers:
      - name: initcontainer-beacon
        image: gcr.io/keep-test-f3e0/keep-random-beacon-hardhat:latest
        imagePullPolicy: Always
        env:
        - name: CHAIN_API_URL
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: http-url
        - name: CONTRACT_OWNER_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: contract-owner-eth-account-private-key
        - name: KEEP_CLIENT_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-account-privatekeys
              key: account-4
        - name: ACCOUNTS_PRIVATE_KEYS
          value: $(CONTRACT_OWNER_ETH_PRIVATE_KEY),$(KEEP_CLIENT_ETH_PRIVATE_KEY)
        - name: KEEP_CLIENT_ETH_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: eth-account-info
              key: account-4-address
        args:
        - initialize
        - --network
        - goerli
        - --owner
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --provider
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --operator
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --amount
        - "800000"
        - --authorization
        - "800000"
      - name: initcontainer-ecdsa
        image: gcr.io/keep-test-f3e0/keep-ecdsa-hardhat:latest
        imagePullPolicy: Always
        env:
        - name: CHAIN_API_URL
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: http-url
        - name: CONTRACT_OWNER_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-network-goerli
              key: contract-owner-eth-account-private-key
        - name: KEEP_CLIENT_ETH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: eth-account-privatekeys
              key: account-4
        - name: ACCOUNTS_PRIVATE_KEYS
          value: $(CONTRACT_OWNER_ETH_PRIVATE_KEY),$(KEEP_CLIENT_ETH_PRIVATE_KEY)
        - name: KEEP_CLIENT_ETH_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: eth-account-info
              key: account-4-address
        args:
        - initialize
        - --network
        - goerli
        - --owner
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --provider
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --operator
        - $(KEEP_CLIENT_ETH_ADDRESS)
        - --amount
        - "800000"
        - --authorization
        - "800000"
---
apiVersion: v1
kind: Service
metadata:
  name: keep-client-4
  namespace: default
  labels:
    app: keep
    type: client
    id: "4"
    network: goerli
spec:
  type: LoadBalancer
  ports:
  - name: network
    port: 3919
    targetPort: network
  - name: metrics
    port: 9601
    targetPort: metrics
  - name: diagnostics
    port: 9701
    targetPort: diagnostics
  selector:
    app: keep
    type: client
    id: "4"
    network: goerli
  loadBalancerIP: null
