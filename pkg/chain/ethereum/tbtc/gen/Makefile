npm_package_name=@keep-network/tbtc-v2

# Contracts for which the bindings should be generated.
# required_contracts := Bridge LightRelay
required_contracts := LightRelay

include ../../common/gen/Makefile
