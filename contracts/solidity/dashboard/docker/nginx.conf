worker_processes 1;

events { worker_connections 1024; }

http {
    sendfile on;
    server {
        include /etc/nginx/mime.types;
        root /var/www/;
        index index.html;
        listen 80;
    }
}